# sift audit — shortest-path-forever @ 937c4ca

Scope: whole repo (branch `cb/ferry` lineage). Gate before: pass. Gate after: pass.

## Carried forward

None (first audit).

## Slices

| Slice | Files read | Reported | Proven | Changed | Unproven | Suppressed | Gate |
|---|---|---|---|---|---|---|---|
| docs | 4/4 | 7 | 0 | 0 | 0 | 0 | pass |
| config | 9/9 | 4 | 3 | 3 | 0 | 0 | pass |
| `tools/` | 7/7 | 16 | 1 | 1 | 2 | 0 | pass |
| `tests/` | 11/11 | 4 | 1 | 1 | 0 | 0 | pass |
| UI leaves | 6/6 | 7 | 1 | 1 | 1 | 0 | pass |
| map | 3/3 | 4 | 2 | 2 | 0 | 0 | pass |
| state + wire | 4/4 | 5 | 0 | 0 | 2 | 0 | pass |
| planner | 3/3 | 9 | 3 | 3 | 0 | 0 | pass |

Envelopes: 73 in `.sift/runs/` (8 slices × lenses). The final `findings.py validate` shows 57 INCOMPLETE,
all caused by "file changed since review", i.e. by this audit's own fixes.

## Changes

| Slice | Change | Category | Lines |
|---|---|---|---|
| `tests/journey_spec.lua` | stub reassignment duplicating journey_driver | test-plumbing | −7 |
| `Map.lua` | impossible nil guard in HideDestinations; stale cluster-key comment | defensive-noise / stale-docs | −1 |
| `.pkgmeta` | ignore entries the packager already prunes (dotfiles) | dead-code | −2 |
| `.github/workflows/release.yml` | stale packager comment (fixed with the SHA pin) | stale-docs | 0 |
| `Arrow.lua` | existence guard on `ns.RefreshCompass` (always defined earlier in the toc) | defensive-noise | −2 |
| `tools/baker/gen_nav.py` | history phrase in a rule comment | comment-narration | 0 |

Net: −11 lines across 5 files (audit commits only; setup added 237 net lines of config/CI).

## Bugs found, not fixed

| Location | Evidence | Suggested fix |
|---|---|---|
| `release.yml` packager pin (setup) | the first pin resolved `v2` to a tag object (nested annotated tag), not a commit | fixed in setup: `e50a250 # v2.6.1` |
| `Core.lua` DEFAULTS vs `Settings.lua` | `mapFlightMasters`, `mapRoutes` have checkboxes defaulting true but no DEFAULTS entry; Map/Route read `ns.db.<key>` directly | add both to DEFAULTS once the owner confirms the intended default |
| `tests/path_spec.lua` | prints `path_spec ok` while every other spec prints `<name>: ok` | add the colon |
| `tests/journey_performance.md:51,70` | cites a harness outside the repo (`~/drive/proj/wow-handoff/…`) | check it in or drop the reference |

## Unproven candidates

| Location | Candidate | What would settle it |
|---|---|---|
| `Path.lua` | unused `endpoint(..., others)` and its loop | dropped on rebase: a later perf commit made the `expansions` counter live (returned by the search), so the change was withdrawn; re-check the `endpoint` parameter alone |
| `Model.lua:62,92` | lift tuning constants belong in the generated fit table | regenerate the data and diff |
| `Sync.lua:110` | re-parse of an already-decoded message | a wire-compat test against the previous release |
| `Compass.lua:31` / `ns.RefreshCompass` | visibility predicate written twice | extract once the owner accepts a shared helper |
| `Tracker.lua:79` vs `Alert.lua` RADIUS | 120 yd duplicated | confirm the two radii mean the same thing |
| `tools/gen_*.py` | duplicated RLE/header/download code, silent fallbacks, hand-rolled opt() | any change alters generated output: regenerate and diff |
| `tools/baker/gen_nav.py:49` | `NAV_SOURCE` env override has no setter in repo | ask the owner whether it is an external knob |
| `Route.lua:271`, `Tracker.lua:5`, `Journey.lua:848` | route kinds / failure reasons as bare strings | project-level constants table decision |
| `Planner.lua:242`, `Journey.lua:673` | oversized functions on the 3 ms frame path | bench before and after any split |

## Learning captured

- Rules added: none (no pattern recurred three times with a mechanical check).
- Lenses added: none.
- Project skill updates: test seams (`Path.after/clock/budget`, `ns.Path == nil`) and the debug trace as live
  roots; generator-emitted strings are data; the packager already prunes dotfiles.

## Next

Confirm the intended defaults for `mapFlightMasters` and `mapRoutes` and add them to `Core.lua` DEFAULTS.
