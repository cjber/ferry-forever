# sift audit — shortest-path-forever @ a8c723a (release pass for v1.2.0)

Scope: diff since the 2026-09-24 audit (734507c..a8c723a: #41 Stormwind walks, #42 teleport icons, #43 flight
wait, #44 stop kinds, #45 corpse run). Single reviewer, no envelopes (release pass). Gate before and after: pass.

## Carried forward

The 2026-09-24 open items are unchanged except:

| Item | Disposition |
|---|---|
| `Path.lua` HasData comment | fixed: `Path.HasData` now lives in `PathGrid.lua` |
| `Arrow.lua`, `Tracker.lua` walk-progress copies of Journey `OnWalk` | still present; `ns.OnWalk` is now exported (#45, for Corpse.lua), but the copies differ (no reach limit, cached lengths, flight legs), so unifying them is a behaviour change needing `/reload` checks |

Every other item in the 2026-09-24 report is still present as described there.

## Files read

`Corpse.lua`, `Looks.lua` in full; the #41-#45 diffs of `API.lua`, `Core.lua`, `Journey.lua`, `JourneySteps.lua`,
`Path.lua`, `Planner.lua`, `Route.lua`, `Settings.lua`, `Tracker.lua`, `types/`; `CHANGELOG.md`, `README.md`,
`docs/curseforge.md`, `docs/features.md`.

## Changes

| Change | Category |
|---|---|
| `CHANGELOG.md` [Unreleased]: the untimed-wait entry still quoted *wait about 2:45*; #43 changed it to *leaves in about*, added *ready in* for teleports and dropped the flight wait | stale-docs |
| Project skill risk order names `Looks.lua` and `Corpse.lua` | learning |

## Left alone

| Location | Candidate | Why |
|---|---|---|
| `Journey.lua` `CorpseRun` `ns.Corpse ~= nil` | guard | live: `tests/journey_driver.lua` loads Journey without Corpse |
| `Corpse.lua` `Search` | computes `Planner.WalkPoints` then overwrites it off-map | cheap, and folding it in reorders the branches; not worth the churn |
