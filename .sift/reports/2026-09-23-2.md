# sift audit — shortest-path-forever @ 5baf646

Scope: whole repo from origin/main. Gate before: pass. Gate after: pass. Planner, Path, Journey,
JourneyInput and the state/wire files also had a blind Codex review; each of its findings was checked
before it was acted on.

## Carried forward

| Item (2026-09-23) | Disposition |
|---|---|
| Core DEFAULTS missing `mapFlightMasters`, `mapRoutes` | fixed in #8; the duplicate list in Settings.lua removed here |
| `path_spec` output colon | fixed in #8 |
| `journey_performance.md` cites the external harness | resolved: it now names `$SPF_HARNESS` and `tests/ui.sh` as external by design |
| `Path.lua` `endpoint(..., others)` | fixed: no caller passes it (Codex agreed) |
| `Model.lua:62,92` lift constants | open; still needs a regenerate-and-diff |
| `Sync.lua:110` re-parse | open; needs a wire-compat test |
| `Compass.lua:31` visibility predicate | open; owner decision |
| `Tracker.lua:79` vs `Alert.lua` radius | open; Tracker now uses 160/120, so the two may differ on purpose |
| `tools/gen_*.py` duplication | open; now provable offline (Data regenerates identically) |
| `gen_nav.py:49` `NAV_SOURCE` | open; owner question |
| Stringly-typed route kinds / failure reasons | open; Codex adds sighting provenance (`Model.lua:220`) and walk-failure codes (`Journey.lua:1187`) |
| Oversized Planner/Journey functions | open; Codex adds a module split for Journey.lua's waypoint ownership |

## Slices

| Slice | Files read | Reported | Proven | Changed | Unproven |
|---|---|---|---|---|---|
| docs + config | 11/11 | 6 | 5 | 5 | 1 |
| tests | 9/9 | 3 | 2 | 2 | 1 |
| UI leaves + map | 10/10 | 2 | 1 | 1 | 1 |
| state + wire (Codex) | 5/5 | 1 + 1 bug | 0 | 0 | 1 |
| planner (Codex) | 4/4 | 5 | 2 | 2 | 2 |

Envelopes: 5 in `.sift/runs/`; at 5baf646 `findings.py validate` reads all five as FINDINGS, with none
INCOMPLETE.

## Changes

| File | Change | Category |
|---|---|---|
| `types/README.md` | names JourneyInput.lua as the file with the second suppression | stale-docs |
| `.luacheckrc` | duplicate `MenuUtil`; "Round N" history labels | dead-code / comment-narration |
| `tests/search_performance.md`, `docs/screenshots.md` | stale spinner description; one session's check results | stale-docs / session-residue |
| `tests/planner_spec.lua` | stops rerunning journey_spec (about 2.3 s per gate) | test-plumbing |
| `Path.lua` | `endpoint`'s never-passed `others` | dead-code |
| `Journey.lua` | unreachable `exactMaps` branch and the `bounded` flag | dead-code |
| `Core.lua`, `Settings.lua`, `types/Addon.lua` | the panel reads `ns.Defaults` instead of restating defaults | parallel-implementations |
| `tests/api_ui.lua` | asserts the #27 ring and numeral pins; it failed on main | stale test |

## Bugs found, not fixed

| Location | Evidence | Suggested fix |
|---|---|---|
| `Sync.lua:54` | an unsolicited `ns.Share` sighting does not mark the route answered, so a request 6 s later is answered on YELL again: identical messages 7.2 s apart against `REPLY_EVERY = 30` (reproduced via the sync_spec stubs) | call `MarkAnswered` for the distribution when sharing |
| external `harness2.lua:1660` | `tests/ui.sh` fails on main: "followed walks must get terrain geometry" | update the harness outside the repo |

## Unproven candidates

| Location | Candidate | What would settle it |
|---|---|---|
| `Path.lua:1768` | the HasData comment describes a height lookup | already removed by open PR #29 |
| `Compass.lua` icon tables vs `ns.SetTransportIcon` | the kind-to-icon names are duplicated | a shared name map; sizing stays separate |
| `tests/*_ui.lua`, `*_bench.lua` | an 8× harness-loading preamble | a small arg[0]-relative loader |
| `tests/journey_performance.md` | a wall of session measurements | condense to the contract plus bench commands |

## Learning captured

- Rules added: none.
- Project skill: the actionlint/zizmor fallbacks; the pinned engine and the `long-function` checker skew;
  `*_ui.lua` as external checks that CI never runs; offline data regeneration; the single settings-defaults
  source; zones for `docs/`, `types/` and the performance notes.
