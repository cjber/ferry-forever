# sift audit — shortest-path-forever @ 0cb1d40

Scope: whole repo from origin/main. Gate before: pass. Gate after: pass (stylua, ruff, shfmt, luacheck,
shellcheck, typecheck, specs, actionlint, zizmor, gitleaks, `agents.py check`). Eleven blind reviewers, one
per slice; every finding was re-proven from the code before any edit.

## Carried forward

| Item (2026-09-23-2) | Disposition |
|---|---|
| `Sync.lua` `ns.Share` does not `MarkAnswered` | still present (now `Sync.lua:119`) |
| external `harness2.lua` scenario failure | still present, identical at origin/main: `harness2.lua:1660` "followed walks must get terrain geometry", after it logs `Path.lua:780: attempt to compare number with nil`; every `tests/*_ui.lua` passes |
| `Path.lua` HasData comment | still present on main; removed by open PR #29, left to avoid a conflict |
| `Model.lua:62,92` lift constants | still present; needs a regenerate-and-diff |
| `Sync.lua:110` re-parse | still present; reviewer confirms the wire bytes would not change, but `sync_spec` walks the upvalue chain |
| `Compass.lua:31` visibility predicate / icon table vs `ns.SetTransportIcon` | still present; the copies now differ (Map errors on an unknown kind, Compass falls back) |
| `Tracker.lua` vs `Alert.lua` 120 yd | still present; Tracker's 160/120 is deliberate hysteresis, a shared constant is an owner call |
| `tools/gen_*.py` duplication | still present; `gen_transit.py --offline` cannot run from the current cache (see project skill) |
| `gen_nav.py` `NAV_SOURCE` | still present; owner question |
| stringly-typed kinds / failure reasons | still present; now in Anti-patterns |
| oversized Planner/Journey | still present; Journey's Guide ownership (164-360) is the clean seam |
| `*_ui.lua` harness preamble ×8 | still present |
| `journey_performance.md` wall | still present; proposal: a budgets summary on top, reports under History |

## Slices

| Slice | Files read | Reported | Proven | Changed | Unproven / left | Gate |
|---|---|---|---|---|---|---|
| docs + config | 29/29 | 8 | 6 | 6 | 2 | pass |
| standards | 19/19 | 4 | 0 | 0 | 4 (proposals) | — |
| tools/baker | 7/7 | 8 | 2 | 2 | 6 | pass |
| tools generators | 10/10 | 5 | 1 | 1 | 4 | pass |
| tests specs | 12/12 | 6 | 3 | 3 | 3 | pass |
| tests harness | 14/14 | 3 | 2 | 2 | 1 | pass |
| UI leaves | 7/7 | 7 | 1 | 1 | 6 | pass |
| map | 3/3 | 4 | 3 | 3 | 1 | pass |
| state + wire | 5/5 | 4 | 2 | 2 | 2 | pass |
| planner + journey | 3/3 | 7 | 1 | 1 | 6 | pass |
| Path.lua | 1/1 | 4 | 0 | 0 | 4 | — |

Envelopes: 11 in `.sift/runs/`, all FINDINGS at 0cb1d40 (60 reported, 0 suppressed, 0 unverifiable).

## Changes

| Slice | Change | Category |
|---|---|---|
| docs | `docs/features.md` switch rule is 30 s *and* 10%; AGENTS.md names every CI check | stale-docs |
| config | `.luarc.json` empty FrameXML library; 12 unused declarations in `types/Forever.lua` | dead-code |
| tools | `rasterize_tile` calls `cluster_box`; `gen_nav.py` globals comments; `render_demo`'s overwritten arg | parallel / comment / dead-code |
| tests | driver `fire`, duplicate Portals load, `NewTicker` stub, `owner.loading` branch, bench `sample` | dead-code / residue |
| UI | `Arrow.lua` `x and` terms after the early return | defensive-noise |
| map | always-false `cached.docks` check; two comments moved beside their code | defensive-noise / comment |
| state | `Model.MIN_SAMPLES`/`MIN_SPAN` as locals; Core ticker comment | dead-code / comment |
| planner | dead store of `undiscovered` in the topology build | dead-code |

Net: −20 lines across 16 files (plus the profile update).

## Bugs found, not fixed

| Location | Evidence | Suggested fix |
|---|---|---|
| `tests/walk_sim.lua:117` | fails "following the drawn route must not flip it" (Auberdine → Tanaris, 1 flip); passes at ec05e01, fails from a93d0ae (#17) on | find whether #17's Guide ownership changed replanning or the sim is stale |
| `Sync.lua:119` | carried: unsolicited share not marked answered | `MarkAnswered` per distribution in `ns.Share` |

Suspected, unproven: `Compass.lua:175` and `Tracker.lua:124` do arithmetic on raw `UnitPosition`, bypassing the
secret-value check in `ns.JourneyPosition`; `Route.lua:392` `Bridge` wraps to `points[#points - 1]` at index 2.

## Unproven candidates

| Location | Candidate | What would settle it |
|---|---|---|
| `Path.lua:1085` | `run()` redoes `pointNode`'s locate+snap | owner OK to change which failure reason wins |
| `Journey.lua:882`, `:735`, `:1047` | switch rule twice; 7 yd/s ×5; `bakedBound` restates Planner keys | land after PR #29 (heavy Journey overlap) |
| `Planner.lua:286` | `revision` cache key only set by runtime_spec | owner: planned data reload? |
| `Arrow.lua:107`, `Tracker.lua:126` | walk-progress copies of Journey `OnWalk` | export `OnWalk`; UI checks |
| `Taxi.lua:36` | `GetTaxiMapID and` guard | a Forever client check |
| `tools/pack_nav.py:16` | legacy chunk conversion never fires | owner: drop the documented path? |
| `gen_nav.py:892,903,822` | hand-rolled `opt()`, `NAV_MAP` fallback, RLE twice | a rebake to diff |
| `gen_transit.py:42`, `screenshots.py:95` | fetch helper twice; START/GOAL in Python and Lua | offline regen / re-render |
| `planner_spec.lua:467`, `compass_spec.lua:2`, `journey_optimal_spec.lua:188` | clone; unused source override; per-row print | owner taste |
| standards | WFA-12 `[Unreleased]` shape, WFA-7 icon, WFA-9 docks mock lacks the click hint, WFA-2 compass off | proposals |

## Learning captured

- Rules added: none (no recurring shape a tool can judge without judgment).
- Project skill: Anti-patterns list; new files in the risk order; walk_sim's known failure; standards-pack path;
  the `ns-once` regex false positive; cache gap for `gen_transit --offline`; live roots for api_ui rebinding,
  `Path.decodes` and `pack_nav`.

## Next

Find which part of a93d0ae (#17) makes walk_sim flip the Auberdine → Tanaris route, and decide whether the sim
or the Guide change is wrong.
