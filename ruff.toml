# The Python under tools/ generates the addon's data; it is run by hand, never shipped (.pkgmeta ignores tools).
line-length = 120
target-version = "py310"

[lint]
select = ["E", "F", "I", "UP", "B"]
